public class Calculations {
    public double calculate(String carType, double loanAmount, int loanYears) {
        // TODO: calculations for everything
        /** GUI gui = new GUI();

        double interestRate = 0;
        carType = gui.carTypeComboBox.getSelectedItem();
        loanAmount = gui.Double.parseDouble(insertLoanAmountField.getText());
        loanYears = gui.Integer.parseInt(insertLoanTextField.getText());

        if(carType == "Imported"){
            if (loanAmount > 300000) {
                interestRate = 0.0235;

            }else if (loanAmount <= 300000) {
                interestRate = 0.0255;

            }else{
                interestRate = 0.0275;
            }
        }

        if(carType == "Local"){
            if (loanAmount > 100000) {
                interestRate = 0.03;

            }else if (loanAmount <= 100000) {
                interestRate = 0.031;

            }else{
                interestRate = 0.032;
            }
        }

    double totalRepayment = 0, totalInterest = 0;
    
    totalInterest = loanAmount * interestRate * loanYears ;
    totalRepayment = totalInterest + loanAmount;

    return totalRepayment;

         */
        return 0;
    }
}
